package xupengofmorethread;

public class MarkWorld简单认识 {
    /**
     * java中每个对象在创建时都会有一个头，默认状态下存储
     *   锁的类型（无锁|重量级锁|轻量级锁|偏向锁）；hashcode值；
     *   GC中的分代年龄 以及锁的标记状态。
     *
     *  随着程序的运行，对象头的内容也会发生变化，比如获得锁后
     *  hashcode值 GC中的分代年龄 将会被代替为对象锁指向的地址引用
     *  当然在该引用中存储了这两个属性的具体值。
     *
     *  如果当前对象的锁是一个偏向锁，那么该对象的头信息中会包括它
     *  所绑定的线程ID。
     */
}
